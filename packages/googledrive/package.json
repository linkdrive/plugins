{
  "name": "@linkdrive/plugin-google-drive",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo \"\" && exit ",
    "changelog": "conventional-changelog -p angular -i CHANGELOG.md -s --commit-path .",
    "release": "node ../../scripts/release.js"
  }
}
